{% extends 'base.html.twig' %}

{% block body %}
    <h1>Biuro</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ biuro.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ biuro.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ biuro.address }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('biuro_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('biuro_edit', { 'id': biuro.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
