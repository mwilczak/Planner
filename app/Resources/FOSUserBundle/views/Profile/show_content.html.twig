{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <p>{{ 'Your username'|trans }}: {{ user.username }}</p>
    <p>{{ 'Your email'|trans }}: {{ user.email }}</p>

    {% if is_granted('ROLE_ADMIN') %} <a href="edit">Edit</a>
    {% endif %}
</div>
